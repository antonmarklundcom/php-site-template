{
  "name": "site-tests",
  "private": true,
  "description": "Playwright screenshots for PR previews. The site itself never depends on Node.",
  "scripts": {
    "shots": "node screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
